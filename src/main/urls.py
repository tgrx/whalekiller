PATH_ROOT = ""

PATH_DOCS = f"{PATH_ROOT}/docs"

PATH_INDEX = f"{PATH_ROOT}/"
PATH_CLOUD = f"{PATH_ROOT}/cloud"
PATH_CLOUD_SETUP = f"{PATH_CLOUD}/setup-cloud"

PATH_API = f"{PATH_ROOT}/api/v1"

PATH_ATTACK = f"{PATH_API}/attack"
PATH_STATS = f"{PATH_API}/stats"
